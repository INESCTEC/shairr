auth.url="http://keycloak:8080/auth"
auth.realm="shairr"
auth.client_id="backend"
auth.client_secret="rk37CmaiaJWm6sBtPOCm4N8bcaUvqaV1"
jwt_secret="JR6NdZwgsjI6wXgnX53c"
hash_salt="Qus0nVRUe1R1socUXvTi"

# Shared folder mounted in both API and worker containers.
SHARED_DATA_ROOT=/shared
# Host path mounted to SHARED_DATA_ROOT in compose.
# When using docker.sock (worker launches docker on host),
# set this to the real host folder (for example /mnt/shared).
# It may differ from SHARED_DATA_ROOT.
SHARED_DATA_HOST_PATH=/shared